# ============================================================================
# Prometrix / VisualClone AI - Consolidated Requirements
# ============================================================================
# This file consolidates all dependencies from:
# - requirements.txt (root)
# - brand registration/requirements.txt
# - backend/requirements.txt
# - backend/requirements-minimal.txt
#
# INSTALLATION (to avoid Rust compiler requirement):
# ============================================================================
# IMPORTANT: Pydantic 2.x requires Rust if building from source.
# To avoid Rust compilation, use pre-built wheels:
#
#   pip install --only-binary :all: -r requirements.txt
#
# This forces pip to use pre-built wheels (available for Windows, macOS, Linux x86_64)
# and avoids compiling from source, eliminating the Rust requirement.
#
# If --only-binary fails, ensure you're on a supported platform or install Rust:
#   Windows: https://rustup.rs/
#   macOS: brew install rust
#   Linux: sudo apt-get install rustc cargo
# ============================================================================

# ============================================================================
# Core Web Framework
# ============================================================================
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
jinja2==3.1.3

# ============================================================================
# Database & ORM
# ============================================================================
sqlalchemy==2.0.25
aiosqlite==0.19.0

# ============================================================================
# Configuration & Environment
# ============================================================================
python-dotenv==1.0.0
# Pydantic 2.x uses Rust for core validation - use --only-binary flag during install
# Pre-built wheels available for Windows, macOS, Linux x86_64 (no Rust needed)
pydantic==2.5.3
pydantic-settings==2.1.0

# ============================================================================
# HTTP Clients
# ============================================================================
httpx==0.26.0
requests>=2.31.0

# ============================================================================
# AI / LLM Providers
# ============================================================================
# OpenAI (required for image analysis, DALL-E, GPT-4 Vision)
openai>=1.12.0

# Anthropic Claude (optional - for alternative LLM provider)
anthropic>=0.7.0

# Google Gemini (optional - for video generation and alternative LLM)
# Note: Code uses 'from google import genai' - ensure google-generativeai is installed
google-generativeai>=0.7.0

# ============================================================================
# PDF & Document Processing
# ============================================================================
# PDF text extraction
PyPDF2==3.0.1

# DOCX processing
python-docx==1.1.0

# OCR support for scanned PDFs (optional - requires Tesseract OCR installed)
pytesseract==0.3.10
pdf2image==1.17.0

# Image processing
Pillow>=10.2.0

# ============================================================================
# File Handling & Utilities
# ============================================================================
aiofiles==23.2.1
python-magic==0.4.27
typing-extensions>=4.9.0

# ============================================================================
# Development & Testing (optional)
# ============================================================================
# Uncomment for development:
# pytest==7.4.3
# pytest-asyncio==0.21.1

# ============================================================================
# INSTALLATION NOTES:
# ============================================================================
# 
# AVOIDING RUST COMPILATION:
# --------------------------
# Pydantic 2.x requires Rust compiler if building from source.
# SOLUTION: Use pre-built wheels (no Rust needed):
#   pip install --only-binary :all: -r requirements.txt
#
# This works on: Windows, macOS (Intel & Apple Silicon), Linux x86_64
# If you're on an unsupported platform, install Rust or use Pydantic 1.x
#
# SYSTEM DEPENDENCIES:
# --------------------
# 1. For OCR functionality (pytesseract), you also need to install Tesseract OCR:
#    - Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
#    - macOS: brew install tesseract
#    - Linux: sudo apt-get install tesseract-ocr
#
# 2. For pdf2image, you may need poppler:
#    - Windows: Download from https://github.com/oschwartz10612/poppler-windows/releases
#    - macOS: brew install poppler
#    - Linux: sudo apt-get install poppler-utils
#
# 3. python-magic may require libmagic:
#    - Windows: Use python-magic-bin instead: pip install python-magic-bin
#    - macOS: brew install libmagic
#    - Linux: sudo apt-get install libmagic1
#
# OPTIONAL DEPENDENCIES:
# ----------------------
# - Anthropic Claude (anthropic>=0.7.0) - optional LLM provider
# - Google Gemini (google-generativeai>=0.7.0) - optional for video generation
# The code handles missing imports gracefully - omit if not needed
# ============================================================================
